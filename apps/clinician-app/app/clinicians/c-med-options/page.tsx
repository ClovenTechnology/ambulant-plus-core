import CMedOptionsClient from './CMedOptionsClient';

export const metadata = {
  title: 'C-Med Kit & Flexible Options | Ambulant+ Clinicians',
  description:
    'Compare the R0 direct training pathway with optional C-Med Flex and C-Med Full offers published by Ambulant+.',
};

export default function CMedOptionsPage() {
  return <CMedOptionsClient />;
}
